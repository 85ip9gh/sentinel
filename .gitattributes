# Files a Linux process parses must keep LF endings regardless of core.autocrlf.
# A CRLF shell script fails with an unreadable "bad interpreter" error, and a
# systemd unit silently mis-parses.
*.sh text eol=lf
*.service text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
